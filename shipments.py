class InventoryAllocator :
    def inventory_distribution(order, inventory):
        result = []
        for i in range(len(inventory)):
            stock = list(inventory[i]['inventory'].keys())
        